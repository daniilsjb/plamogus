version: '3'
services:
  database:
    image: 'postgres:15-alpine'
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: 'uap'
      POSTGRES_USER: 'uap_user'
      POSTGRES_PASSWORD: 'uap_user'
