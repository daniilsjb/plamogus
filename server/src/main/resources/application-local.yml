database:
  url: 'jdbc:postgresql://localhost:5432/uap'
  username: 'uap_user'
  password: 'uap_user'

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: always